import { BorderCommonButton } from '@styles/buttons';
import styled from 'styled-components';

export const DraftItem = styled(BorderCommonButton)`
  display: flex;
  justify-content: space-between;
  font-size: 14px;
`;
